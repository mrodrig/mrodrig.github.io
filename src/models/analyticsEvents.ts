export enum AnalyticsEvent {
    Click = 'click',
    Download = 'download',
    GenerateLead = 'generate_lead',
    Hover = 'hover',
    Login = 'login',
    NotFound = 'not_found',
    PageView = 'page_view',
    Search = 'search',
    SelectContent = 'select_content',
    Share = 'share',
}

export enum AnalyticsNavigationEvent {
    ScreenView = 'screen_view',
}
